{
  "name": "LSDJS",
  "version": "0.1",
  "description": "A Javascript library for visual odometry on browsers",
  "main": "LSDVO.js",
  "browser": "./dist/lsdvo.min.js",
  "scripts": {
    "devbuild":"tsc && ./node_modules/.bin/esbuild ./dist/index.js --bundle --outfile=dist/debug.js --global-name=LSD --minify --sourcemap",
    "build":"tsc && ./node_modules/.bin/esbuild index.js --bundle --outfile=dist/bundle.js --global-name=LSD --minify"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "keywords": [
    "LSDSVO"
  ],
  "author": "3fffff",
  "license": "",
  "devDependencies": {
    "esbuild": "0.20.2"
  },
  "dependencies": {
    "typescript": "^5.4.2"
  }
}
